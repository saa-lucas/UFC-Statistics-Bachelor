### 🧪 **1h30 – Tratamento de dados com erros reais**

| Tempo  | Bloco                                  | Conteúdo                                       |
| ------ | -------------------------------------- | ---------------------------------------------- |
| 10 min | 📁 Carregamento e diagnóstico inicial  | `read_csv`, `info()`, `describe()`, `isnull()` |
| 20 min | 🔧 Tipos errados e conversões          | `astype`, `to_numeric`, tratamento de textos   |
| 15 min | ❌ Valores faltantes                    | `fillna`, `dropna`, lógica de preenchimento    |
| 15 min | 🔁 Duplicatas e inconsistência textual | `drop_duplicates`, `str.strip()`, `replace()`  |
| 15 min | 🚨 Outliers e erros lógicos            | `query`, `loc`, `apply`, `replace`             |
| 15 min | 🎯 Revisão do DataFrame limpo          | mostrar o `head()` e `info()` do final         |

---

### **1h – Visualização final com matplotlib e seaborn**

| Tempo  | Bloco                            | Conteúdo                                          |
| ------ | -------------------------------- | ------------------------------------------------- |
| 10 min | Boxplot (antes vs depois)        | `seaborn.boxplot()`                               |
| 15 min | Histograma ou barplot            | `seaborn.histplot()` ou `matplotlib.pyplot.bar()` |
| 15 min | Customização de cores            | Paletas do Seaborn + link para consultar cores    |
| 10 min | Salvando gráficos                | `plt.savefig()`                                   |
| 10 min | Fechamento com comparação visual | Mostrar lado a lado: gráfico sujo vs limpo        |

---

### 10m – Espaço para Dúvidas:

---

Nota do último alinhamento:

- O **minicurso** mostra o **“como fazer”**.
- O **relatório** mostra o **“por que isso importa”**.****